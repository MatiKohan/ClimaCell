const getData = ( lat, lon ) => {
    const data = {};
    return data;
}

const getSummary = ( lat, lon ) => {
    const summary = {};
    return getSummary;
}

const addWeatherForecasts = () => {

}

module.exports = {
    getData,
    getSummary,
    addWeatherForecasts
}